#!/bin/bash
set -euo pipefail
git branch --sort=-committerdate --format="%(HEAD) %(refname:strip=2):|  %(creatordate:relative):|  %(contents:subject)" | head -n 10 | column -s ':' -t | cut -c -$(tput cols)
